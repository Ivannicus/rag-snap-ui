{
  "rules": {
    "sessions": {
      "$sessionId": {
        ".read": "auth != null && auth.token.email.matches(/.*@canonical\\.com$/)",
        ".write": "auth != null && auth.token.email.matches(/.*@canonical\\.com$/)"
      }
    },
    "teamMembers": {
      ".read": "auth != null && auth.token.email.matches(/.*@canonical\\.com$/)",
      ".write": "auth != null && auth.token.email.matches(/.*@canonical\\.com$/)"
    },
    "savedFiles": {
      ".read": "auth != null && auth.token.email.matches(/.*@canonical\\.com$/)",
      ".write": "auth != null && auth.token.email.matches(/.*@canonical\\.com$/)"
    },
    "rfpDatabase": {
      ".read": "auth != null && auth.token.email.matches(/.*@canonical\\.com$/)",
      ".write": "auth != null && auth.token.email.matches(/.*@canonical\\.com$/)"
    }
  }
}
